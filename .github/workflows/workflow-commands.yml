name: workflow commands

on:
  push:
    branches:
      - main

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: group logging
        run: |
          echo "::group::My Grouped Messages"
          echo "msg 1"
          echo "msg 2"
          echo "::endgroup::"
