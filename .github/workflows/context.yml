name: Context Examples

on: ["push"]

jobs:
  my-context-job:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        env:
          MY_ACTION: ${{ github.action }}
        run: echo "Hello $MY_ACTION"

